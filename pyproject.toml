[build-system]
requires = ["hatchling>=1.27"]
build-backend = "hatchling.build"

[project]
name = "mediaforce"
version = "0.1.0"
description = "Manifest-driven AV1 planning and execution workflow for Mediaforce v2"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
  "fastapi",
  "uvicorn",
]

[project.scripts]
mediaforce = "media_harness.cli:main"
mediaforce-web = "media_harness.web.app:main"
media-harness = "media_harness.cli:main"
media-harness-web = "media_harness.web.app:main"

[tool.hatch.build.targets.wheel]
packages = ["media_harness"]

[tool.hatch.build.targets.wheel.force-include]
"frontend/build" = "media_harness/web/frontend_build"
