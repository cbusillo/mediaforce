#!/usr/bin/env bash

set -euo pipefail

ROOT_DIR="$(cd "$(dirname "$0")/.." && pwd)"

while IFS= read -r variable; do
  unset "$variable"
done < <(git rev-parse --local-env-vars)

exec "${ROOT_DIR}/scripts/pre-commit-check.sh"
